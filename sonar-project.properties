# must set the token generated by sonar
sonar.login=

# must be unique in a given SonarQube instance
sonar.projectKey=br.jus.stj.justica:nomeprojeto-web

# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=nomeprojeto-web
sonar.projectVersion=1.0.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set.
sonar.sources=src/app/

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

sonar.host.url=https://sonarqube.stj.jus.br
sonar.exclusions=**/*.spec.ts,**/*breadcrumb.*,**/app.*,**/core/**,**/shared/mocks/**
sonar.typescript.lcov.reportPaths=coverage/lcov.info
